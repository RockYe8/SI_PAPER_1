# Context

## Glossary

### 模拟考试模式

一种答题流程：考生必须完成一整套试卷的全部 60 道选择题后，系统才公布分数、正确答案、错题解释和来源。

### 试卷批次

一组编号对应的题目文件与答案文件，例如 `exam_001` 与 `answer_001`。每个试卷批次包含 60 道题。

### 错题解释

考生提交试卷后，对答错题目展示的解析文本，来源于对应 `answer_xxx.md` 中该题的 Explanation 与 Source。

### 公开题库网站

一个通过公开 GitHub 仓库发布的静态考试网站。题目、答案与解析文件都视为公开内容；界面可以延后显示答案，但不承诺对答案做保密。

### React/Vite 考试应用

用于承载公开题库网站的前端应用形态。它负责展示试卷列表、管理 60 题答题状态、提交后计分，并显示正确答案、错题解释和来源。

### 题库目录

所有试卷批次文件统一存放在 `exams/` 文件夹中。构建流程只扫描该目录下成对出现的题目文件与答案文件。

### 粤语中文题干

题目文件中的中文内容采用粤语中文表达，属于正式题目内容。构建和展示流程应保留英文题干与粤语中文题干，不应因为本地终端显示异常而判定为乱码。

### 双语固定展示

考试页面默认同时展示英文题干与粤语中文题干，不提供语言切换。

### 提交前可修改

考生在提交前可以在 60 道题之间自由跳转、跳过题目，并修改已选答案。提交后答案锁定，系统进入结果展示状态。

### 完成后提交

考生必须完成全部 60 道题后才能提交试卷。若仍有未答题目，系统提示剩余未完成题数，并阻止提交。

### 考试限时

每套试卷总时长为 90 分钟。考试页面显示剩余时间。

### 超时自动提交

当 90 分钟倒计时结束时，系统自动提交当前答案，即使仍有未答题目。未答题目在结果中按错误处理。

### 结果页展示

提交后，结果页展示全部 60 道题的正确/错误状态、考生选择与正确答案。只有错题默认展开显示 Explanation 与 Source；答对题目不默认展开解析。

### 本地进度保留

考试进度保存在浏览器本地存储中，包括当前试卷、已选答案、考试开始时间与提交状态。刷新或重新打开页面后可以恢复进度；倒计时根据原始开始时间继续计算，不因刷新而重置。

### 重新开始试卷

已提交或进行中的试卷可以由考生通过明确按钮重新开始。重新开始会清空该试卷的本地答案、提交状态与开始时间，并重新进入 90 分钟倒计时。

### 试卷状态

试卷列表展示每套试卷的本地状态，包括未开始、进行中、已完成与已超时提交。

### 分数展示

结果页显示答对题数与百分比，例如 `47 / 60` 和 `78.3%`。

### 固定题序

模拟考试模式按 Markdown 文件中的 Question 1 到 Question 60 固定顺序展示，不随机打乱。

### 单选答案

每道题最终只允许在 A、B、C、D 四个选项中选择一个答案。即使题干包含 I/II/III/IV 组合判断，页面交互仍是 A/B/C/D 单选。

### 试卷目录

一个显式列出可用试卷批次的清单文件，推荐文件名为 `exams/index.json`。网站通过试卷目录展示可选择的试卷，而不是猜测服务器上有哪些文件。
